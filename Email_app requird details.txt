EMAIL CHECKER WEBSITE REQUIRED DETAILS

Programming Language : Python 3.10.8

Django Version : 5.0.1

MySQl Client version : 3.3.8

MySQL WorkBench 8.0 CE



To Downlaod  Python 3.10.8  https://www.python.org/downloads/release/python-3108/


Command to install Django:pip install Django==5.0.1



Note:Following these credentials, along with this, you want to download VSCode and extensions like Django, HTML CSS support, etc.














# from django.core.validators import validate_email                            {#PHASE 1}
# from django.core.exceptions import ValidationError
# from django.shortcuts import render
# from django.http import HttpResponse

# def email_check(request):
#     valid_emails = []
#     invalid_emails = []

#     if request.method == 'POST':
#         emails = request.POST.get('emails', '').split(',')

#         for email in emails:
#             try:
#                 validate_email(email.strip())  
#                 valid_emails.append(email)
#             except ValidationError:
#                 invalid_emails.append(email)

#     return render(request, 'emailapp/email_check.html', {'valid_emails': valid_emails, 'invalid_emails': invalid_emails})


# from django.core.validators import validate_email                            {#PHASE 2}
# from django.core.exceptions import ValidationError
# from django.shortcuts import render

# def email_check(request):
#     valid_emails = []
#     invalid_emails = []

#     if request.method == 'POST':
        
#         emails = request.POST.get('emails', '').split()

#         for email in emails:
#             try:
#                 validate_email(email.strip()) 
#                 valid_emails.append(email)
#             except ValidationError:
#                 invalid_emails.append(email)

#     return render(request, 'emailapp/email_check.html', {'valid_emails': valid_emails, 'invalid_emails': invalid_emails})











